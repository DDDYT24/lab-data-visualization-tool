[project]
name = "labviz-api"
version = "2.1.1"
description = "FastAPI processing service for the LabViz V2.1 web application"
requires-python = ">=3.12,<3.14"

[tool.pytest.ini_options]
addopts = "-q"
pythonpath = ["."]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.ruff.lint.per-file-ignores]
"labviz_api/main.py" = ["B008"]

[tool.mypy]
python_version = "3.12"
strict = true
warn_unused_ignores = true

[[tool.mypy.overrides]]
module = ["boto3.*", "botocore.*", "matplotlib.*", "numpy.*", "openpyxl.*", "pandas.*", "pyarrow.*", "scipy.*"]
ignore_missing_imports = true
