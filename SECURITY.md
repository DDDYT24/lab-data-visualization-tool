# 安全说明

LabViz 的默认部署是单机本地运行。安全边界包括本机操作系统账户、磁盘权限、备份介质、
浏览器会话以及是否有人能够访问本机的 3000/8000 端口。

## 报告安全问题

请通过 GitHub 的私密 Security Advisory 报告可复现的安全问题；如果仓库未开启该入口，
请先在公开 Issue 中只留下“不包含敏感细节的联系请求”，不要粘贴实验数据、密钥、数据库文件、
登录验证码或可访问的分享链接。报告应包含受影响版本、运行系统、复现步骤和预期/实际结果。

## 使用本地部署时

- 默认绑定 `127.0.0.1`，不要在没有 HTTPS、身份认证和访问控制的情况下开放端口。
- 将 `V2.0/api/.labviz/` 和备份视为敏感数据目录。
- 不要把 `.env`、远程数据库凭据、对象存储凭据或真实实验数据提交到 Git。
- 依赖安装和升级应使用仓库中的锁定文件，并在升级后运行完整验证。
- 发现数据已暴露时，立即停止服务、收回分享链接、保护或更换相关凭据，并保留时间线。

云端、多主机和远程对象存储属于维护者自行配置的扩展部署，不是普通本地用户的必要组件。
